name: Send Follow-up Emails

on:
  schedule:
    - cron: "30 8 * * *"   # 2:00 PM IST
  workflow_dispatch:

jobs:
  send-followups:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger follow-up command
        run: |
          curl -X POST "${{ secrets.FOLLOWUP_URL }}"